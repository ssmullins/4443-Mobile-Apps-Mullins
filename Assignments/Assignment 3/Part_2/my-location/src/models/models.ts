/// ### Use when you have to take user input ### \\\


// Will be able to use this contructor in any page. You just have to import them
export interface User{
    first_name: string;
    last_name: string;
    email: string;
    password: string;
}
